services:
  backend:
    build: .
    ports:
      # Bound to localhost only: the API manages broker keys.
      - "127.0.0.1:5001:5001"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/backend/data
      - ./backend/strategies:/app/backend/strategies
    restart: unless-stopped
