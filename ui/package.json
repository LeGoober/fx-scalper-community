{
  "name": "fx-scalper-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Vanilla TypeScript + Web Components dashboard for the ICT × Jev × Deriv platform",
  "type": "module",
  "scripts": {
    "build": "tsc -p . && node scripts/copy-static.mjs",
    "watch": "tsc -p . --watch",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "lightweight-charts": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
